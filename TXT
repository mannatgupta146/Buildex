// export server, not app fix this ny it is a dns issue .. very common let me tell you the fix .. import express from 'express'; import { createProxyMiddleware } from 'http-proxy-middleware'; import http from 'http'; const app = express(); // Route by path instead of subdomain app.use('/agent/:agentId', (req, res, next) => { const { agentId } = req.params; createProxyMiddleware({ target: http://localhost:YOUR_AGENT_PORT, // resolve internally ws: true, // enable WebSocket proxying changeOrigin: true, pathRewrite: { [^/agent/${agentId}]: '' }, })(req, res, next); }); const server = http.createServer(app); // Also proxy WS upgrades server.on('upgrade', (req, socket, head) => { // handle ws upgrade here }); server.listen(3000); this is the code which i have genereted via ai ... you can take ref form here to fix you dns issue .. or you can generate a custom code for your code .. using ai .. it's a DNS issuecurl.exe -i "http://019e72c9-974a-7108-b6f1-f2ed3761c499.agent.localhost/socket.io/?EIO=4&transport=polling"