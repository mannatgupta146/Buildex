FROM node:20-alpine

WORKDIR /workspace

COPY package*.json /opt/template-deps/

RUN mkdir -p /opt/template-deps && npm install --prefix /opt/template-deps

COPY . .

EXPOSE 5173

CMD ["sh", "-c", "/opt/template-deps/node_modules/.bin/vite"]